To download the material do the following from a _login_ node:
```bash
git clone https://gitlab.hpc.cineca.it/training/summer-school.git
```

This command will not work from a compute (i.e. backend) node.